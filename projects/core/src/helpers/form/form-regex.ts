export const MIN_HEIGHT_CHARACTERS_PATTERN = /^.{8,}$/;

export const MIN_ONE_UPPER_CASE_CHARACTER_PATTERN = /^(?=.*?[A-Z]).*$/;

export const MIN_ONE_LOWER_CASE_CHARACTER_PATTERN = /^(?=.*?[a-z]).*$/;

export const MIN_ONE_DIGIT_PATTERN = /^(?=.*?\d).*$/;

export const MIN_ONE_SPECIAL_CHARACTER_PATTERN = /^(?=.*?[!@#$%^*()_\-+{};:.,]).*$/;
